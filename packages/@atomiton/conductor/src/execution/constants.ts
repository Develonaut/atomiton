/**
 * Execution constants and defaults
 */

/**
 * Default slow-mo delay per progress step (in milliseconds)
 * This controls how long each progress update step takes during node execution
 */
export const DEFAULT_SLOWMO_MS = 250;
