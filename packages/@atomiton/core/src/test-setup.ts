import { beforeAll, afterAll, afterEach } from "vitest";

beforeAll(() => {
  console.log("Starting @atomiton/core tests");
});

afterAll(() => {
  console.log("Completed @atomiton/core tests");
});

afterEach(() => {
  // Clean up any test artifacts or reset state if needed
});
